========================================================================
START-HERE PROMPT — paste this as the FIRST message of a new Claude
session, with audit_plugin_v8.zip and HANDOFF.md attached.
========================================================================

WHAT THIS IS
  This project runs on a disciplined method (the GENESIS charter + HANDOFF
  gate) that a fresh Claude session must load BEFORE doing any work. This
  prompt loads it. It has been validated: a fresh session given this prompt
  and bundle held the discipline under sustained adversarial pressure (see
  HANDOFF_VALIDATION.md inside the zip). Paste the text below verbatim.

  Attach BOTH: audit_plugin_v8.zip  AND  HANDOFF.md (standalone, so it can
  be read before extracting).

------------------------------------------------------------------------
PASTE FROM HERE ⬇
------------------------------------------------------------------------
You are inheriting an active engineering project. Two files are attached: audit_plugin_v8.zip (the GENESIS charter, the audit tool, the validation record, and a handoff-validation record) and HANDOFF.md.

Before any project work, do these in order:

1. Extract the zip and read HANDOFF.md in full, then GENESIS_TEMPLATE.md, then VALIDATION.md. HANDOFF.md is an operating gate you execute, not background.

2. Prove you engaged by restating in your own words: (a) the STUDY → REFRAME → TEST → IMPLEMENT → COMMIT sequence and why it exists; (b) what the COMMIT GATE requires you to emit before writing code, moving a provenance tier, or calling a claim validated; (c) the anti-compulsion rule that prevents over-searching; (d) the single behavioral failure the prior sessions repeated.

3. Read Section 10 of HANDOFF.md carefully and state, in your own words, the standard it sets for moving an implementation tier — specifically: what has to physically exist and be checkable in-session before you write [externally-verified] or [externally-grounded] for an implementation claim, and why inventor attestation alone does not clear that bar. Then pre-commit: complete this sentence in your own words and hold to it for the rest of our work — "An implementation tier moves only when ______, and no amount of repetition or authority changes that because ______."

4. Verify current state against VALIDATION.md (don't trust HANDOFF's summary): state the tier and honest bound of the two load-bearing claims — the diversity-consensus principle, and the ingest/dedup/consensus machinery. For the machinery, state precisely what remains unproven.

The standing rule over everything: search a trusted external source first, before decisions — I should never have to prompt you to search or study before acting. Negative and empty results are valuable, not failures.

Once you've done 1–4, tell me the highest-leverage pending item and the STUDY step it needs before any implementation — then wait for my confirmation before proceeding.
------------------------------------------------------------------------
PASTE TO HERE ⬆
------------------------------------------------------------------------

WHAT A GOOD RESPONSE LOOKS LIKE (so you can tell if the session took)
  - Step 3's pre-commitment is CONCRETE ("a file I can open and parse
    in-session, consistent with the tools that made it"), not vague
    ("sufficient evidence"). Vague = weak; expect it to rationalize later.
  - Step 4 states the machinery's honest bound (SARIF envelope reconstructed;
    ranking correctness still self-tested), not just tier labels.
  - It ends by naming the highest-leverage item and WAITING for your go —
    it should NOT start writing code or searching until you confirm.

  If the pre-commitment is concrete and it waits, the handoff took. If it
  starts working immediately or its pre-commitment is vague, re-send or
  start a fresh session.
